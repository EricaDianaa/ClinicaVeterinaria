@model IEnumerable<ClinicaVeterinaria.Models.Cassetto_Prodotti>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cassetti.IdCassetto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prodotti.Tipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prodotti.Nome)
        </th>
        <th>Gestione</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cassetti.IdCassetto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prodotti.Tipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prodotti.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.IdCassettoProdotti }) |
            @Html.ActionLink("Details", "Details", new { id = item.IdCassettoProdotti }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.IdCassettoProdotti })
        </td>
    </tr>
}

</table>
